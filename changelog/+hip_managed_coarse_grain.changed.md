Mark managed allocations on HIP unified-memory devices coarse-grained. This keeps them readable from the CPU after a device synchronization, as before, while allowing hardware float atomics to operate on them. Two costs: buffers written by the CPU while a kernel is concurrently running are no longer coherent without that synchronization, and each managed allocation carries an extra ~140 µs.
